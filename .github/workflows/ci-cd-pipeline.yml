name : brigdeql-test-and-build

on: push

jobs:
  test-build:
    name: test and build bridgeql
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["2.7", "3.7", "3.8", "3.9"]
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Move to project directory
        run: cd ${{ github.workspace }}

      - name: Make tests
        run: make test

#       - name: swtich to virtual enironment and run tox
#         run: source venv/bin/activate && tox

#       - name: Upgrage build to latest version
#         run: python -m pip install --upgrade build

#       - name: run library module as a script 
#         run: python -m build
